<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
	PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- 加载属�?�文�? -->
	<properties resource="jdbc.properties"></properties>
	
	<!-- 全局的设�? -->
	<settings>
		<!-- �?启缓�? -->
		<setting name="cacheEnabled" value="true"/>
		<!-- 启用延迟加载功能 -->
		<setting name="lazyLoadingEnabled" value="true"/>
		<!-- 按需要延迟加�? -->
		<setting name="aggressiveLazyLoading" value="false"/>
	</settings>
	
	<!-- 配置类型别名 -->
	<typeAliases>
		<!-- 采用包扫描方式：扫描该包下所有的类都为MyBatis的类型别�? -->
		<package name="org.fkjava.hrm.domain"/>
	</typeAliases>
	
	<!-- 配置MyBatis3的的环境 -->
	<environments default="project">
		<environment id="project">
			<!-- 配置事务管理�? -->
			<transactionManager type="JDBC"/>
			<!-- 配置数据�? (用MyBatis3自带的连接池)-->
			<dataSource type="POOLED">
				<!-- 驱动类名 -->
				<property name="driver" value="${driver}"/>
				<!-- URL -->
				<property name="url" value="${url}"/>
				<!-- 用户�? -->
				<property name="username" value="${username}"/>
				<!-- 密码 -->
				<property name="password" value="${password}"/>
			</dataSource>
		</environment>
	</environments>
	
	<!-- 配置数据库表访问的接口或映射文件 -->
	<mappers>
		<!-- 采用包扫描方式：扫描该包下所有的接口类都为MyBatis的数据访问接�? -->
		<package name="org.fkjava.hrm.mapper"/>
	</mappers>

</configuration>
